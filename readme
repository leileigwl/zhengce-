

   - 在 `README.md` 文件中添加项目的基本信息，例如项目名称、描述、安装说明、使用方法等。以下是一个示例模板：

```markdown
# 项目名称

## 项目描述
这是一个用于抓取政策信息的 Python 脚本，能够从指定的页面获取数据并导入到 Notion。

## 安装
1. 克隆这个仓库：
   ```bash
   git clone https://github.com/yourusername/yourrepository.git
   cd yourrepository
   ```

2. 安装依赖：
   ```bash
   pip install -r requirements.txt
   ```

## 使用
1. 配置 `config.txt` 文件，确保包含正确的 `page_id` 和 `token`。
2. 运行脚本：
   ```bash
   python nengyuan.py
   ```
   或
   ```bash
   python policies_scraper.py
   ```

## 贡献
欢迎任何形式的贡献！请提交问题或拉取请求。

## 许可证
此项目采用 MIT 许可证 - 详细信息请参见 [LICENSE](LICENSE) 文件。
```
